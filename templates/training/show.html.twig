{% extends 'base.html.twig' %}

{% block title %}Détails
	{{ training }}
{% endblock %}

{% block main %}
<p>Modules de la formation : 
{% for module in training.durations %}
{{ module }}
{% endfor %} <a href="{{ path('add_duration', {'id': training.id}) }}" class="btn btn-warning btn-sm" role="button">Modifier les modules</a></p>
{% endblock %}
