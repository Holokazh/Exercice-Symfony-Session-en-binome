{% extends "base.html.twig" %}

{% block main %}
	<a href="{{ path('app_login') }}">Retour<a>

			<h1>Récupération de mot de passe</h1>
			<form name="change_password" method="post">
				{{ form_row(formResetPassword.newPassword) }}
				{{ form_row(formResetPassword.valider) }}
				{{ form_row(formResetPassword._token) }}
			</form>
		{% endblock %}
